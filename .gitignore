# Dependencies
node_modules/

# Build output (the project's own — anchored to the root, so a vendored
# dependency that happens to contain a `dist/` directory is still committed;
# `public/vendor/embedded-app-sdk/lib/uuid/dist/` was silently dropped by the
# unanchored pattern, which only CI's asset check noticed)
/dist/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / Editor junk
.DS_Store
Thumbs.db
.idea/
.vscode/

# Cloudflare Workers local secrets
.dev.vars
